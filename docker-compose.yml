version: "3.9"
services:
  mongodb:
    image: "mongo"
    container_name: "mongodb"
    command: -vv
    ports: 
      - "27017:27017"
  web-api:
    build:
      dockerfile: Dockerfile-api
    ports:
      - "20001:20001"
    links: 
      - "mongodb"
  web-client:
    build:
      dockerfile: Dockerfile-client
    ports: 
      - "3000:3000"
