FROM node:latest
WORKDIR /usr/src/app
COPY api/package*.json ./

RUN npm install
# RUN npm ci --only=production
COPY api .
# RUN npm install pm2 -g
RUN npm install -g nodemon


EXPOSE 20001
CMD ["npm", "run", "dev"]